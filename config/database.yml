# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem "sqlite3"
#
default: &default
  adapter: postgresql
  encoding: unicode
  username: penn            # Replace with your PostgreSQL username
  password: Jupercho11      # Replace with the actual password
  host: localhost
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: pennjunior_development

test:
  <<: *default
  database: pennjunior_test

production:
  <<: *default
  database: pennjunior_production
  username: penn
  password: <%= ENV['PENN_DATABASE_PASSWORD'] %>
